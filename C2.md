# C2：单截融合 `y += (xA)B`

分支：`c2-sgmv-fused-lora`（从 `c1-sgmv-shrink-ntr` 分出，不沿用 C1 的 3D gather / Python slice）。源码提交 **`69eec5d`**（`1edc110` 上补了 `void*` 声明和 bench `--op`）。

目标：相对 **B1**（21.0 ms / 44.4 tok/s）降低 decode TPOT，向 **B0**（17 ms / 55.6）走近。不是把 C1 修回 B1。

## 做什么

每个 LoRA slice **一次** Vector 核：

1. UB 里 `t = scale * (x @ A)`（原 shrink）
2. 不写出 `[T,R]` 到 GM
3. 同一核 `y[:, off:off+H] += t @ B`（原 expand）

Decode 上 shrink+expand 从约 1184 launch 变成约 **592**。FSL 下 A 在 `set_lora` 时 all_gather 成满 rank（生产每卡 2 → 8），decode **不再**对激活做 3D/逐截 gather。

Rank 必须是 8/16/32/64（与现有 expand 的 BlockReduce 一致）。对不上则回退 B1 的 shrink+expand。

**没有**把基座 MatMul 和 LoRA 融在一起（那是以后的 C5）。一层里多截仍是每截一次 launch。

## 改了哪些文件

| 文件 | 作用 |
|---|---|
| `csrc/kernels/sgmv_lora.cpp` | 新核 `sgmv_lora_*` / `bgmv_lora_*` |
| `csrc/ops.h` | `sgmv_lora_impl` / `bgmv_lora_impl` |
| `csrc/torch_binding.cpp` | 绑定 |
| `csrc/torch_binding_meta.cpp` | meta |
| `vllm_ascend/lora/lora_ops.py` | Python 封装 |
| `vllm_ascend/lora/punica_npu.py` | `add_lora_linear` 走融合，失败回退 B1 |
| `vllm_ascend/lora/utils.py` | `set_lora` 反分片 A；FSL `_mcp_apply` 不再 gather 激活 |

未改 `sgmv_expand.cpp`。C1 的 shrink CopyOut 仍在树里，C2 热路径不走 packed shrink。

`__global__` 声明必须写成 `__gm__ void* x`（不要 `void *x`）。cann-9.0.0 auto_gen 会把后者解析成会写 `*x` 的调用，和核签名对不上。

## 编译

必须编 **kernels + C 绑定**（新了 `sgmv_lora.cpp`）。禁止 `setup.py`。禁止只拷 punica 不编核。

生产机（cann-9.1.0，沿用已有 `build_c1` 的生成器，不要为了 C2 改成 Ninja）：

```bash
source /usr/local/Ascend/cann-9.1.0/set_env.sh
export CMAKE_BUILD_PARALLEL_LEVEL=1 MAKEFLAGS=-j1
cmake --build build_c1 --target vllm_ascend_kernels --parallel 1
cmake --build build_c1 --target vllm_ascend_C/fast --parallel 1
cp build_c1/lib/libvllm_ascend_kernels.so vllm_ascend/
cp build_c1/vllm_ascend_C*.so vllm_ascend/
```

测试机没有 9.1.0 时只 source `cann-9.0.0`，不要混 toolkit。`ccec` / `ld.lld` 必须落在同一套 CANN。

从零 cmake 时：

- SOC 用 `npu-smi` 看型号（910B 常见 `ascend910b1` / `ascend910b4`）。
- **CMake 4 + Ninja + cann-9.0.0 会炸**：`extract_host_stub.py` 对带 `./` 的 object 路径 KeyError；`recompile_binary.py` 要 Makefile 才有的 `link.txt`。不要改 C2 源码、不要改 cann 脚本、不要给 `compile_commands.json` 打补丁。处理：`rm -rf build_c1` 后 **`-G "Unix Makefiles"`**，`-j1` 从零编。
- 生产 9.1.0 未必同样炸；第一次编核若 Ninja 失败，同样先换 Make，不要动算法。
- 增量链接会把旧树残留 `.o` 再链进 `kernels.so`。编前删 `get_masked_input_and_mask*.o`。`nm` 要有 `aclrtlaunch_sgmv_lora_*`，不能有 masked 未定义符。
- 看到 `[238/259]` 立刻停（走错 aclnn 全量）。编 C 用 `vllm_ascend_C/fast`，不要无 `/fast` 的 `vllm_ascend_C`。

`.so` 必须拷到 `python3 -c "import vllm_ascend"` 的那个目录。镜像里常有一份官方 vllm-ascend，C2 在另一份 clone，不设 `PYTHONPATH` 会加载旧包。两个 `.so` 必须新于 `sgmv_lora.cpp`。

## 验收（生产 27B）

同一 decode 窗：

| 项 | 通过 |
|---|---|
| `sgmv_lora` / `bgmv_lora` | 约 **592**（或接近 slice 数） |
| `sgmv_shrink` + `sgmv_expand` 热路径 | 应接近 **0**（回退则失败） |
| 新 Cat / 3D gather | 不能再冒出来 |
| 数据集 | **TPOT < 21.0 且 tok/s > 44.4**（相对 B1） |

达不到就报不通过。TTFT 不当成功。不要写拉回 B0。单卡倍数不是生产 tok/s。

**T>1 单卡精度没过之前，不要上 27B。** Decode 一个 step 也会把 batch 里多个 token 打进同一次 `bgmv_lora`，只过 T=1 不够。

## 单卡最小验证

脚本：`bench_c2_kernel.py`（精度 fused vs shrink+expand，T=1/32，rank=8，hidden=5120）。

`--op shrink|expand|lora|all`。Health 不是 OK 就停。容器里常见只有 `npu:0`，`npu-smi` 的编号可能是 1、7，不要写死。容器不能 `npu-smi reset`。

复位 / 换卡后必须先 `--op shrink`（旧核探活），再 expand，再 lora。shrink 也挂 → 卡没好；shrink 过、lora 挂 → 才是 C2 核问题。

### 2026-08-26 上午：NPU 7 Alarm，不通过

检出 `1edc110`。cann-9.0.0。旧容器、已有 `build_c1`。编核过（void* + 删残留 `.o`）。`.so` 能 load，`torch.zeros` 能过。一进 bench 卡在 `rtStreamSynchronize`，AICore 0%，进程 D 态。tokens=1 零输出 exit 124。NPU 7 Health=Alarm。容器不能 reset。

### 2026-08-26 下午：另一张卡，T=32 精度炸，不通过

新容器 `bca34f3be01d`。源码仍是 **`69eec5d`，一行没改**。只有 `build_c1/` 和拷进去的 `.so`。没上 27B，没改 C1，没 setup.py。

| 项 | 值 |
|---|---|
| 系统 | Ubuntu 22.04.5 |
| CANN | 9.0.0（未混 toolkit、无 9.1.0） |
| Python / torch | 3.12.13，torch 2.10.0+cpu，torch_npu 2.10.0 |
| 卡 | `/dev/davinci1` → torch `npu:0`（不是上一轮 NPU 7） |
| 型号 | 910B3，`SOC_VERSION=ascend910b1` |
| Health | 全程 OK |
| 仓库 | 另克隆 `/root/vllm-ascend`；镜像自带 `/vllm-workspace/vllm-ascend` 是官方 v0.21.0rc1，必须 `PYTHONPATH` 指新树 |
| 生成器 | CMake 4.3.2 + Ninja 在 9.0.0 上失败；**Unix Makefiles** 从零编过 |
| `.so` | 2026-08-26 08:46:07 UTC，新于源 08:22:07；nm 有 `aclrtlaunch_sgmv_lora_bfloat16_t` / `_half` |

| 档 | 返回 | 结果 |
|---|---|---|
| `--op shrink --tokens 1` | 是 | `t.abs.max=116.0873` |
| `--op expand --tokens 1` | 是 | `y.abs.max=592` |
| `--op lora --tokens 1` | 是 | `y.abs.max=424` |
| `--op all --tokens 1` | 是 | **max_abs_err=0**；ref 0.0791 ms，fused 0.0404 ms，**1.96x** |
| `--op all --tokens 32` | 是 | **max_abs_err=2.0**（要远小于 1e-2）→ 炸；ref 0.1000 ms，fused 0.0512 ms，1.95x |

卡是好的，核编出来了，shrink/expand/lora 都能返回，没有再卡同步。fused 墙钟快约 2 倍，**这是本镜像核级倍数，不是生产 tok/s，不能换算成 44.4→55.6。**

T=1 逐元素一致、T=32 炸：多 token 分核 / 同核连续处理 / 索引，不是 CANN 版本也不是 Alarm。生产 decode batch>1 会走多 token，所以必须先修这个。

## 下一步（先做这个，不要上 27B）

1. **同一份 `/root/vllm-ascend` + 已编好的 `.so`，不要删 `build_c1`，不要换 Ninja。**
2. 把 bench 打出的 `worst_token` / `token0_max` / `n_tokens_gt_1e-2` / argmax 记下来。再扫 `--tokens 2,4,8,16`：T=2 就炸 → 多 AIV 并发；只有 T 大到 `num_tokens_per_core>1` 才炸 → 同核连续 token 的 UB/队列没清干净。
3. decode 形态再跑 `bgmv_lora`（每 token 一个 index），不要只看 `seq=[T]` 的 sgmv。
4. 按上面的模式改 `sgmv_lora.cpp`（`Process` 分核、`CopyInIndex`、token 之间 TPipe/tBuf），再只编 kernels + `C/fast`。T=1 和 T=32 的 `max_abs_err` 都要远小于 1e-2 才算单卡过。
5. 单卡过了再上生产 27B（cann-9.1.0、现有 `build_c1` 生成器、先 `--op shrink` 探活）。不要把这次「Ninja + 改 json」带到生产。
