# C2：单截融合 `y += (xA)B`

分支：`c2-sgmv-fused-lora`（从 `c1-sgmv-shrink-ntr` 分出，不沿用 C1 的 3D gather / Python slice）。

核：`sgmv_lora.cpp` 相对 `69eec5d` 只改了两个常量——`SHRINK_TILE` 和 `W_IN_TILE_NUM_ELEMENTS` 均为 **8192**（对齐 shrink 一次归约、expand 的 W tile）。不要改回 4096。

目标：相对 **B1**（21.0 ms / 44.4 tok/s）降低 decode TPOT，向 **B0**（17 ms / 55.6）走近。不是把 C1 修回 B1。

## 做什么

每个 LoRA slice **一次** Vector 核：

1. UB 里 `t = scale * (x @ A)`（原 shrink）
2. 不写出 `[T,R]` 到 GM
3. 同一核 `y[:, off:off+H] += t @ B`（原 expand）

Decode 上 shrink+expand 从约 1184 launch 变成约 **592**。FSL 下 A 在 `set_lora` 时 all_gather 成满 rank（生产每卡 2 → 8），decode **不再**对激活做 3D/逐截 gather。

Rank 必须是 8/16/32/64（与现有 expand 的 BlockReduce 一致）。对不上则回退 B1 的 shrink+expand。

**没有**把基座 MatMul 和 LoRA 融在一起（那是以后的 C5）。一层里多截仍是每截一次 launch。

## 改了哪些文件

| 文件 | 作用 |
|---|---|
| `csrc/kernels/sgmv_lora.cpp` | 新核 `sgmv_lora_*` / `bgmv_lora_*` |
| `csrc/ops.h` | `sgmv_lora_impl` / `bgmv_lora_impl` |
| `csrc/torch_binding.cpp` | 绑定 |
| `csrc/torch_binding_meta.cpp` | meta |
| `vllm_ascend/lora/lora_ops.py` | Python 封装 |
| `vllm_ascend/lora/punica_npu.py` | `add_lora_linear` 走融合，失败回退 B1 |
| `vllm_ascend/lora/utils.py` | `set_lora` 反分片 A；FSL `_mcp_apply` 不再 gather 激活 |

未改 `sgmv_expand.cpp`。C1 的 shrink CopyOut 仍在树里，C2 热路径不走 packed shrink。

`__global__` 声明必须写成 `__gm__ void* x`（不要 `void *x`）。cann-9.0.0 auto_gen 会把后者解析成会写 `*x` 的调用，和核签名对不上。

## 编译

必须编 **kernels + C 绑定**（新了 `sgmv_lora.cpp`）。禁止 `setup.py`。禁止只拷 punica 不编核。

生产机（cann-9.1.0，沿用已有 `build_c1` 的生成器，不要为了 C2 改成 Ninja）：

```bash
source /usr/local/Ascend/cann-9.1.0/set_env.sh
export CMAKE_BUILD_PARALLEL_LEVEL=1 MAKEFLAGS=-j1
cmake --build build_c1 --target vllm_ascend_kernels --parallel 1
cmake --build build_c1 --target vllm_ascend_C/fast --parallel 1
cp build_c1/lib/libvllm_ascend_kernels.so vllm_ascend/
cp build_c1/vllm_ascend_C*.so vllm_ascend/
```

测试机没有 9.1.0 时只 source `cann-9.0.0`，不要混 toolkit。`ccec` / `ld.lld` 必须落在同一套 CANN。

从零 cmake 时：

- SOC 用 `npu-smi` 看型号（910B 常见 `ascend910b1` / `ascend910b4`）。
- **CMake 4 + Ninja + cann-9.0.0 会炸**：`extract_host_stub.py` 对带 `./` 的 object 路径 KeyError；`recompile_binary.py` 要 Makefile 才有的 `link.txt`。不要改 cann 脚本、不要给 `compile_commands.json` 打补丁。处理：`rm -rf build_c1` 后 **`-G "Unix Makefiles"`**，`-j1` 从零编。
- 生产 9.1.0 未必同样炸；第一次编核若 Ninja 失败，同样先换 Make，不要动算法。
- 增量链接会把旧树残留 `.o` 再链进 `kernels.so`。编前删 `get_masked_input_and_mask*.o`。`nm` 要有 `aclrtlaunch_sgmv_lora_*`，不能有 masked 未定义符。
- 看到 `[238/259]` 立刻停（走错 aclnn 全量）。编 C 用 `vllm_ascend_C/fast`，不要无 `/fast` 的 `vllm_ascend_C`。

`.so` 必须拷到 `python3 -c "import vllm_ascend"` 的那个目录。两个 `.so` 必须新于 `sgmv_lora.cpp`。只拷 punica、不编核，等于没上 C2。

## 验收（生产 27B）

同一 decode 窗，相对 **B1**（21.0 ms / 44.4 tok/s）：

| 项 | 通过 |
|---|---|
| `sgmv_lora` / `bgmv_lora` | 约 **592**（或接近 slice 数） |
| `sgmv_shrink` + `sgmv_expand` 热路径 | 应接近 **0**（回退则失败） |
| 新 Cat / 3D gather | 不能再冒出来 |
| 数据集 | **TPOT < 21.0 且 tok/s > 44.4** |

达不到就报不通过。TTFT 不当成功。不要写拉回 B0。单卡 2x **不是**生产 tok/s。

## 单卡最小验证

脚本：`bench_c2_kernel.py`（fused vs shrink+expand，rank=8，hidden=5120）。`--op shrink|expand|lora|all`。容器里用实际可见的 `npu:0`，不要写死 `npu:7`。

### 2026-08-26 上午：NPU 7 Alarm，不通过

检出 `1edc110`。旧容器。`.so` 能 load，一进 bench 卡在 `rtStreamSynchronize`，NPU 7 Health=Alarm。tokens=1 零输出。容器不能 reset。**和精度无关。**

### 2026-08-26 下午：另一张卡，未改 tile 时 T=32 看起来炸

新容器 `bca34f3be01d`，NPU 1 / 910B3 / Health OK，源码 **`69eec5d`（tile 仍 4096）**。Unix Makefiles 从零编过。shrink/expand/lora 都能返回。

unseeded T=1 曾得到 err=0；seed=0 下 T=1 也有 0.0625（1 个 bf16 ULP），T=16/32 的 2.0 是同一类：argmax `(t=14,h=1383)`，`|y|≈358` 时 ulp=2。不是漏 token、不是多 AIV、不是 `CopyInIndex`。

原因：C2 把 shrink 切成 4096+1024 两次 ReduceSum，expand 的 W tile 是 4096；对照核是 shrink 一次 11776、expand W=8192。结合律不同 → 差 1 ulp。

### 2026-08-26 晚：改 tile 后单卡精度通过

只改 `sgmv_lora.cpp` 两个常量（相对 `69eec5d`），未改 C1 / punica / `num_tokens_per_core`：

- `SHRINK_TILE`：4096 → **8192**（H=5120 一次 shrink）
- `W_IN_TILE_NUM_ELEMENTS`：4096 → **8192**（对齐 `sgmv_expand.cpp`）

seed=0，sgmv 与 bgmv 相同，T=1/2/4/8/16/32 全部 `max_abs_err=0`，`n_tokens_gt_1e-2=0`。原先炸的点 `y[14,1383]=-358` 与 ref 一致。

| 档 | max_abs_err | 墙钟 |
|---|---|---|
| tokens=1 | **0** | ref 0.0984 ms，fused 0.0488 ms，**2.02x** |
| tokens=32 | **0** | ref 0.0960 ms，fused 0.0494 ms，**1.94x** |

`.so` 2026-08-26 09:09:44 UTC，新于源 09:05:34。nm 有 `aclrtlaunch_sgmv_lora_*`。Health 全程 OK。

**这是 cann-9.0.0 / torch 2.10 测试镜像上的核级倍数，不是生产 27B tok/s，不能换算成 44.4→55.6，也不是拉回 B0。**

## 下一步（生产 27B，不要再扫单卡 T）

单卡精度已过。测试机停。生产机：

1. 拉 `c2-sgmv-fused-lora`（含 8192 tile，不要停在 `69eec5d`）。
2. 只 source **cann-9.1.0**，沿用现有 `build_c1` 生成器，`-j1`，编 **kernels + `vllm_ascend_C/fast`**。禁止 setup.py、禁止 Ninja+改 json。
3. 两个 `.so` 和 punica / `lora_ops.py` / `utils.py` 拷到实际 `import vllm_ascend` 的目录。`nm` 有 `sgmv_lora`。卡 4–7 Health 不是 OK 就停。
4. 同一负载相对 B1：热路径 `sgmv_lora`≈592、shrink+expand≈0、**TPOT < 21.0 且 tok/s > 44.4**。TTFT 不当成功。达不到就报不通过，回退 B1，不要宣称拉回 B0。
