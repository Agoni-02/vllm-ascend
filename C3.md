# C3：MatMul 与 C2 LoRA 双流重叠

**不要改 `c2-sgmv-fused-lora`。** C2 冻结在 **`62f1acb`**。本分支：`c3-matmul-lora-overlap`。

目标：相对 C2（TPOT **20.3** / **46.1 tok/s**）再往 B0（**17 ms / 55.6**）靠。不是修 C1，不是再融 shrink/expand。

## 做什么

保留 C2：`sgmv_lora`、满 rank A、`_c2_try_fused`。

去掉 C1-exp / C1-pack 热路径：不再 `pack_lora_a`、不再 3D shrink+gather 回退。融合打不上就走 **B1** 的 `_mcp_apply` / shrink+expand。

重叠：同一层 Linear 上

1. 当前 stream：`aclnnMatMul`（Cube）写出 `y = xW`
2. 另一条 NPU stream：`sgmv_lora` 写到独立的 `delta`（从 0 累加，不读 `y`）
3. `main.wait` 之后 `y += delta`

关重叠：`export VLLM_ASCEND_C3_OVERLAP=0`（退回 C2 串行：先 MatMul 再 LoRA 打在 `y` 上）。

## 改了哪些文件

相对 C2 只动 **`vllm_ascend/lora/utils.py`**。核、binding、punica 的 `sgmv_lora` **不改**。C2 的 `.so` 可继续用，**不必重编 kernels**。

## 风险（先实验）

窗里 19.6 ms vs 6.9 ms 是合计，不是一次 20 秒对 7 秒。一层里 MatMul 约 65 μs、LoRA 约 12 μs。  
`aclnnMatMul` 收尾可能占用 AIV，Vector LoRA 仍排队，重叠等于 0。验收看 **TPOT**，不要看图上还有没有 `sgmv_lora`。

## 验收（同一 27B 负载）

| 项 | 通过 |
|---|---|
| C2 热路径 | 仍有 `sgmv_lora` ≈ 592（重叠核还在） |
| 相对 C2 | **TPOT < 20.3 且 tok/s > 46.1** |
| 相对 B1 | 仍须好于 21.0 / 44.4 |
| `VLLM_ASCEND_C3_OVERLAP=0` | 应回到 C2 数字 |

达不到就报不通过，设 `OVERLAP=0` 回 C2。TTFT 不当成功。不要写拉回 B0。AIV 没空就不要在双流上耗，改真融合。

## 生产落地

在已经跑过 C2 的树上 **只覆盖 `utils.py`**（本分支或 `c3-prod-overlay/patched/utils.py`）。不要 checkout 覆盖掉 C2 核。不要叠 C1-exp。

## 单卡正确性（2026-08-27）

未改 C2 核、未上 27B。`utils.py` 有 `_c3_apply_overlap`，无 `pack_lora_a_inplace`。kernels `.so` 仍是 C2（2026-08-26 09:09:44 UTC）。热路径 `is_prefill=True`，`sgmv_lora=True`。

| 项 | 结果 |
|---|---|
| Health | 全程 OK（npu:0 / 卡 1），无 Alarm、无 D 态 |
| OVERLAP=0 | 返回。apply `(1,5120)` bf16，absmax=764 |
| OVERLAP=1 | 返回，未卡 `rtStreamSynchronize`。absmax=768 |
| max_abs_err(y1,y0) | **4.0**（mean 0.24） |

按「bf16 远小于 1e-2」**字面不通过**。LoRA 增量两边都在 ~762，差 4.0 出现在 |y|≈764 上，这个量级 bf16 的 ULP 就是 4（相对约 0.5%）。串行是核内 `y=xW` 再 `+=lora`，重叠是独立 `delta` 再 `y+=delta`，结合律不同，**不像没打 LoRA**。

未测生产 TPOT。单卡时间不是 tok/s。**未上生产。**

下一步：若接受 1 ULP 再上生产比 C2 的 TPOT；不接受就把最后一次 `y+=delta` 改成 fp32 再 cast，或停在 C2。AIV 是否藏住 LoRA，测试机这次**没有**回答。
